spring:
  datasource:
   url: jdbc:mysql://localhost:3306/facio?useSSL=false 
   username: root
   password: root
  jpa:
   show-sql: true
   hibernate:
      ddl-auto: update
   properties:       
    hibernate:
      dialect: org.hibernate.dialect.MySQL5Dialect
   
   